# i3 config file (v4)

set $mod Mod4

# fonts
font pango:DejaVu Sans Mono 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"            

# switch to workspace
bindsym $mod+1 workspace 1: web
bindsym $mod+F1 workspace 1: web
bindsym $mod+2 workspace 2: vim
bindsym $mod1+F2 workspace 2: vim
bindsym $mod+3 workspace 3: terms
bindsym $mod+F3 workspace 3: terms
bindsym $mod+4 workspace 4: files
bindsym $mod+F4 workspace 4: files
bindsym $mod+5 workspace 5: top
bindsym $mod+F5 workspace 5: top
bindsym $mod+6 workspace 6: diff
bindsym $mod+F6 workspace 6: diff
bindsym $mod+7 workspace 7: media
bindsym $mod+F7 workspace 7: media
bindsym $mod+8 workspace 8: office
bindsym $mod+F8 workspace 8: office
bindsym $mod+9 workspace 9: graphics
bindsym $mod+F9 workspace 9: graphics
bindsym $mod+0 workspace 10: virtual
bindsym $mod+F10 workspace 10: virtual

# move focused container to workspace
bindsym $mod+Shift+exclam      move container to workspace 1: web
bindsym $mod+Shift+F1          move container to workspace 1: web
bindsym $mod+Shift+at          move container to workspace 2: vim
bindsym $mod+Shift+F2          move container to workspace 2: vim
bindsym $mod+Shift+numbersign  move container to workspace 3: terms
bindsym $mod+Shift+F3          move container to workspace 3: terms
bindsym $mod+Shift+dollar      move container to workspace 4: files
bindsym $mod+Shift+F4          move container to workspace 4: files
bindsym $mod+Shift+percent     move container to workspace 5: top
bindsym $mod+Shift+F5          move container to workspace 5: top
bindsym $mod+Shift+asciicircum move container to workspace 6: diff
bindsym $mod+Shift+F6          move container to workspace 6: diff
bindsym $mod+Shift+ampersand   move container to workspace 7: media
bindsym $mod+Shift+F7          move container to workspace 7: media
bindsym $mod+Shift+asterisk    move container to workspace 8: office
bindsym $mod+Shift+F8          move container to workspace 8: office
bindsym $mod+Shift+parenleft   move container to workspace 9: graphics
bindsym $mod+Shift+F9          move container to workspace 9: graphics
bindsym $mod+Shift+parenright  move container to workspace 10: virtual
bindsym $mod+Shift+F10         move container to workspace 10: virtual



# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

bar {
    mode dock
        position top
        status_command py3status -i ~/.config/i3/py3status -c ~/.config/i3/py3status/config
        colors {
            background #101010
                statusline #999999
                urgent_workspace #2e3436 #9a878f
        }
}

##
# system
##
# set $mode_system (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
bindsym $mod+Shift+E mode "System"
mode "System" {
        bindsym l exec ~/.config/i3/script/i3exit.sh lock, mode "default"
        bindsym e exec ~/.config/i3/script/i3exit.sh logout, mode "default"
        #bindsym s exec ~/.config/i3/script/i3exit.sh suspend, mode "default"
        #bindsym h exec ~/.config/i3/script/i3exit.sh hibernate, mode "default"
        bindsym r exec ~/.config/i3/script/i3exit.sh reboot, mode "default"
        bindsym Shift+Q exec ~/.config/i3/script/i3exit.sh shutdown, mode "default"

# back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+Shift+S mode "Services"
mode "Services" {
    bindsym a exec ~/.i3/i3service.sh apache_start, mode "default"
        bindsym Shift+A exec ~/.i3/i3service.sh apache_stop, mode "default"

        bindsym s exec ~/.i3/i3service.sh mysql_start, mode "default"
        bindsym Shift+S exec ~/.i3/i3service.sh mysql_stop, mode "default"

        bindsym m exec ~/.i3/i3service.sh mongo_start, mode "default"
        bindsym Shift+M exec ~/.i3/i3service.sh mongo_stop, mode "default"

        bindsym v exec ~/.i3/i3service.sh vpn_start, mode "default"
        bindsym Shift+V exec ~/.i3/i3service.sh vpn_stop, mode "default"

# back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+Shift+P mode "Projects"
mode "Projects" {
    bindsym d exec ~/.i3/i3projects.sh drupal_start, mode "default"
        bindsym Shift+D exec ~/.i3/i3projects.sh drupal_stop, mode "default"

        bindsym k exec ~/.i3/i3projects.sh ks_start, mode "default"
        bindsym Shift+K exec ~/.i3/i3projects.sh ks_stop, mode "default"

        bindsym f exec ~/.i3/i3projects.sh file_start, mode "default"
        bindsym Shift+F exec ~/.i3/i3projects.sh file_stop, mode "default"

# back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+Shift+M mode "Mount"
mode "Mount" {
    bindsym m exec ~/.i3/i3mount.sh mount, mode "default"
        bindsym u exec ~/.i3/i3mount.sh umount, mode "default"

# back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Disable Caps Lock
# xmodmap -e "remove lock = Caps_Lock"
# exec setxkbmap -option ctrl:nocaps

# Change language double shift
# exec setxkbmap -option grp:switch,grp:shifts_toggle,grp_led:scroll us,ir

# start urxvt as a daemon
# exec urxvtd -q -o -f

exec i3-msg 'workspace 4: files; exec urxvt -e ranger'
exec i3-msg 'workspace 3: terms; exec urxvt'
exec i3-msg 'workspace 2: vim; exec urxvt -e vim'
exec i3-msg 'workspace 1: web; exec google-chrome'

#background image
exec --no-startup-id feh --no-xinerama --bg-scale /home/sadegh/dotfile/background/bg.jpg
 
# Screenshot
bindsym 0xff61 exec scrot  ~/pictures/screenshots/screenshot_%Y_%m_%d_%H_%M_%S.png
bindsym $mod+p exec scrot  ~/pictures/screenshots/screenshot_%Y_%m_%d_%H_%M_%S.png
 
#screen brightness 
bindsym XF86MonBrightnessDown exec xbacklight -dec 20
bindsym XF86MonBrightnessUp exec xbacklight -inc 20


#kebyboard LED  brightness 
bindsym XF86KbdBrightnessDown exec "~/.config/i3/script/leds_down.sh"
bindsym XF86KbdBrightnessUp exec "~/.config/i3/script/leds_up.sh"

#volume adjustment
set $sink alsa_output.pci-0000_00_1b.0.analog-stereo
bindsym XF86AudioRaiseVolume exec --no-startup-id "pactl set-sink-mute $sink 0; pactl set-sink-volume $sink +2% "
bindsym XF86AudioLowerVolume exec --no-startup-id "pactl set-sink-mute $sink 0; pactl set-sink-volume $sink -2% "
bindsym XF86AudioMute exec --no-startup-id "pactl set-sink-mute $sink toggle "

# bind software
bindsym $mod+Return exec urxvt
bindsym $mod+Shift+Return exec urxvt -e tmux
bindsym $mod+t exec urxvt -e htop
bindsym $mod+x exec urxvt -e ranger
bindsym $mod+o exec libreoffice
bindsym $mod+m exec exec urxvt -e vim
bindsym $mod+i exec iceweasel
bindsym $mod+c exec google-chrome
#bindsym $mod+d exec meld
#bindsym $mod+f exec filezilla
  
